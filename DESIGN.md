# DESIGN.md

## 项目定位

量子科技人才招聘网站第一版前端 Demo —— 面向量子科技候选人的高端人才展示门户，核心体验路径：品牌认知 → 了解量子价值 → 选择技术赛道 → 查看岗位机会 → 进入岗位详情 → 投递或咨询。

## 气质与意象

**Quantum Precision × Laboratory Calm**

整体像"量子实验室中的精密观测"：深邃、克制、干净、稀少光线。以缓慢流动的干涉波纹、柔和折射与极细线条表达量子波函数 / 场 / 干涉现象的抽象感，而非太空或科幻宇宙。呈现前沿科研机构般的可信度与沉静力量。

## 视觉策略

- 首屏 Hero 为「量子叠加抽象视觉系统」：3～4 层半透明不规则体积（Orb/Blob）通过 blur + mix-blend 重叠干涉，叠加细颗粒噪点与静态椭圆轨道线，用颜色抽象表达 Superposition / Probability / Interference，不使用任何真实物理实验照片。
- Hero 双主题（同一视觉系统的两套状态，非简单反色）：
  - Light：暖白纸面底 `#f7f4ee`；体积色为深蓝 `#2b48eb`、朱红 `#ff4a2e`、琥珀 `#ffb03a`、浅蓝 `#7aa7ff`，`multiply` 混色形成油墨式干涉。
  - Dark：深黑蓝底 `#070a12`（禁纯黑）；体积色为蓝 `#3b6ef6`、青 `#22d3ee`、紫 `#8b5cf6`、洋红 `#ec4899`，`screen` 混色形成柔光干涉。
- Hero 动效为「呼吸」：8～15s 周期、scale 0.98↔1.02、位移 ±10～20px、层间错相位；鼠标仅造成 ±8px 内微响应，禁止视差与持续旋转。
- Hero 交互惊喜仅在用户主动时暴露：点击 / 轻触出现 2～3 条椭圆粒子轨迹组成的概率云扩散（0.8～1.5s 消散），移动端仅保留 Tap 反馈。禁止鼠标群聚跟随粒子（SwarmCursor 类效果已废弃）。
- 动效克制、丝滑、低调，不抢文字、不影响性能；遵循 `prefers-reduced-motion`（Hero 降级为静态海报，关闭粒子爆发）。
- 通过主题感知渐变遮罩保证文字可读，不添加蓝紫滤镜。
- 内容区以深色为底、浅色文字为主，营造沉浸阅读感。
- 不使用星球 / 银河 / 星空 / 宇宙背景，不使用廉价 3D 球体、发光原子、抽象宇宙图案。量子科技 ≠ 太空科技。

## Hero 文案

- 主标题：量子科技人才招聘平台（直接说明"这是什么"）。
- 副标题：汇集量子计算、量子通信、量子测量等方向的优质岗位机会。
- 英文 eyebrow：`QUANTUM TALENT PLATFORM`，小字号宽字距，克制。

## 配色方案

- **深空背景**：`#0a0a0b` / `#111113` — 舱外黑暗。
- **舱壁炭灰**：`#1a1a1d` / `#232326` — 卡片与面板。
- **月面白**：`#f5f5f7` — 主标题与正文。
- **舷窗微光**：`#a1a1aa` / `#71717a` — 次要文字。
- **地平线金**：`#d4a574` / `#e8c39e` — 点缀色，来自地球边缘的暖光。
- **液态玻璃**：`rgba(255, 255, 255, 0.06)` / `rgba(255, 255, 255, 0.10)` — 导航与关键面板。

## 字体排版

- 中文主体：系统无衬线栈（PingFang SC、Hiragino Sans GB、Microsoft YaHei）。
- 英文辅助：系统无衬线栈，小字号、字距略宽，用于赛道英文名与标签。
- 标题：大字号、短句、清晰层级；Hero 标题使用极克制字重对比。
- 正文：行高 1.7～1.8，段落不堆叠营销语言。

## 间距与圆角

- 页面大留白：Section 垂直间距 `py-24` ~ `py-32`（桌面），`py-16` ~ `py-20`（移动）。
- 容器最大宽度 `max-w-7xl`，两侧留白充足。
- 卡片圆角 `rounded-2xl`，按钮圆角 `rounded-full` 或 `rounded-xl`。
- 液态玻璃面板使用 `border border-white/10` + `backdrop-blur-xl`。

## 动效与交互

- 滚动触发：轻量 fade-in + translate-y（`opacity: 0 → 1`, `translateY: 24px → 0`），使用 Intersection Observer，不劫持滚动。
- 首页完整首屏 Section 使用 `scroll-snap-type: y proximity`，其余内容自然滚动。
- 液态玻璃 Hover：轻微亮度提升与细边框高光变化，过渡 `0.3s ease`。
- 按钮状态：默认 / Hover / active / disabled / loading 全部覆盖。
- 移动端：保持自然上下滑动，禁用复杂视差。

## 组件质感

- **液态玻璃**仅用于：顶部导航、核心 CTA 按钮背景、投递/咨询面板。
- 玻璃效果：半透明背景 + backdrop blur + 柔和边缘高光 + 细边框 + hover 光泽。
- 保证文字对比度 ≥ 4.5:1。

## 设计禁忌

- 禁止满屏蓝紫渐变与霓虹发光。
- 禁止 Web3 风格、抽象原子/量子球 3D 装饰。
- 禁止首页 Hero 使用星球 / 银河 / 星空 / 宇宙 / 太空背景等"太空科幻"视觉。
- 禁止所有卡片使用玻璃拟态。
- 禁止密集堆砌岗位卡片。
- 禁止通用 AI SaaS 模板式圆角大卡片 + 图标 + 三段文字布局。
- 禁止滚动劫持与移动端复杂视差。
